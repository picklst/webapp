module.exports = {
    API: {
        providers: [
            {
                key: "localhost",
                endpoint: '/api/',
                token_prefix: 'JWT '
            }
        ]
    },
};