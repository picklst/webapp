module.exports = {
    API: {
        providers: [
            {
                key: "localhost",
                endpoint: '/api/graphql/',
                token_prefix: 'JWT '
            }
        ]
    },
};